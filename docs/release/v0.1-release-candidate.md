# Nuxt Email v0.1 release-candidate record

Status: **not approved for publication**. The local release candidate can be prepared and verified, but final approval remains blocked on the candidate identity, package-name ownership, manual email-client QA, and external beta evidence below.

## Candidate identity

| Field | Value |
| --- | --- |
| Version | `0.1.0` |
| Release-candidate commit | Pending final candidate freeze |
| Package tarball | Pending final package build |
| Package tarball SHA-256 | Pending final package build |
| Working package name | `nuxt-email` |
| Publish-name ownership | **Pending confirmation** |

`emailkit` was a specification codename and the final npm name was never approved. The repository currently uses `nuxt-email`; an npm registry probe returned HTTP 200 for that name, but ownership and package metadata could not be verified within the available network permissions. Do not rename in this release task and do not publish until the owner confirms the final name, registry ownership/access, and package metadata.

## Frozen v0.1 contract

The authoring surface is the E-prefixed Nuxt auto-components `EHtml`, `EHead`, `EBody`, `EPreview`, `EContainer`, `ESection`, `ERow`, `EColumn`, `EHeading`, `EText`, `ELink`, `EImg`, `EHr`, and `EButton`. Templates are ordinary Vue SFCs using Vue props, attributes, slots, `defineProps()`, `v-if`, and `v-for`.

The only application rendering API is the generated, server-only `renderEmail(name, props)`. Template names and props come from recursive `app/emails/` discovery, and success returns exactly `{ html, text }`. There is no provider adapter, send endpoint, alternate renderer, public registry protocol, metadata API, or configuration surface in v0.1. Development preview consumes the same registry and renderer.

API review result:

- Names are Vue/Nuxt-native and consistently E-prefixed; React shorthand props are rejected.
- Defaults are explicit where meaningful: `EHtml` uses `dir="ltr"` and `lang="en"`, `EHeading` defaults to `h1`, and `ELink`/`EButton` default to `target="_blank"`.
- Required destinations and image accessibility fields fail early; invalid document roots, unsafe content-replacement/event attributes, unknown templates, duplicate template names, discovery failures, and rendering failures are covered by actionable errors.
- `UnknownEmailTemplateError` reports the requested name and sorted known names. `EmailRenderError` identifies the template and preserves the original cause.
- Types and runtime rendering are generated from one server registry. The API is not auto-imported into client code, and production bundle assertions cover renderer/template client exclusion and development-preview exclusion.

No concrete API issue found in this review requires an immediate post-v0.1 breaking change. Any later provider, metadata, Tailwind, configuration, or registry proposal must pass its documented decision gate.

## React Email oracle

| Field | Pinned value |
| --- | --- |
| `react-email` | `6.9.0` |
| `@react-email/render` | `2.1.0` |
| React / React DOM | `19.2.7` / `19.2.7` |
| Source checkout commit | `6eb428924c4c2774228a07cbec1977ad8898f143` |
| Published package commit | `71656573fa24b09e48173ae2357bf712fcb401b6` |
| Oracle SHA-256 | `0a7b89e1d7e3df885db527ea68f4dbbbfdaa6d0bce3d200c80f416fa3e0a2a99` |

The generated [conformance report](../conformance/report.md) is authoritative for individual source references, classifications, semantic checks, and results. It currently records 30/30 runnable behaviors passing: 10 exact, 9 normalized, 5 semantic, and 6 intentional divergences.

## Supported and unsupported surface

Supported primitives are the fourteen E-prefixed components listed in the frozen contract. Conformance also covers complete document rendering and plain-text conversion, including nested lists, ordered-list starts, blockquotes, hard breaks, tables, preformatted text, links, Unicode, preview exclusion, and Outlook-safe button text.

The following React Email components are explicitly unsupported in v0.1:

| React Email component | v0.1 decision |
| --- | --- |
| `CodeBlock` | Syntax-highlighted code output is outside the focused primitive set. |
| `CodeInline` | Inline code styling is outside the focused primitive set. |
| `Font` | Font-loading behavior requires separate email-client evidence. |
| `Markdown` | Markdown parsing is not required for native Vue SFC authoring. |
| `Tailwind` | Deferred until its post-v0.1 entry gate passes. |

This is not a claim of global or percentage compatibility. The exact six intentional divergences—`html-defaults`, four preview cases, and `row-columns`—and their Vue/email-safety reasons live only in the generated [conformance report](../conformance/report.md). Closely translated MIT algorithms and release notice requirements are recorded in [provenance](../conformance/provenance.md).

## Evidence ledger

| Evidence | Current record | Final-candidate action |
| --- | --- | --- |
| Oracle reproducibility | `pnpm oracle:check` passed on 2026-07-20. | Re-run after candidate freeze. |
| Conformance | `pnpm conformance:check` passed on 2026-07-20; generated report records 30/30 behaviors passing. | Re-run and require an unchanged report. |
| Determinism and performance | The fixed 48-row fixture produced byte-identical output across all 1,100 warm/sequential renders; timing, size, environment, and heap observations are in the [Phase 3 measurement](../performance/phase-3.md). | Re-run only if renderer behavior changes; it is an informational baseline, not a threshold. |
| Unit/component/module/type checks | Implemented in the repository suites; `pnpm lint` passed on 2026-07-20. | Run the complete documented verification command on the frozen commit and attach its log. |
| Production bundle boundary | `pnpm exec vitest run test/production-boundary.test.ts` passed 3/3 cases on 2026-07-20. It builds application fixtures, executes production server render chunks, rejects client `renderEmail` imports, and requires templates/renderer/preview/fixtures to be absent from client or production output as applicable. | Re-run on the frozen commit and attach its log. |
| Package contents and fresh install | Pending final candidate tarball. | Inspect the tarball, verify licenses/notices, install that exact tarball twice in fresh Nuxt apps, and record both under-ten-minute results. |
| Email clients | **Pending external QA.** | Complete the fixed [manual email-client QA record](../testing/manual-email-client-qa.md) for Gmail web, Apple Mail, and Outlook for Windows. |
| Real projects | **Pending external beta.** | Complete at least one [external beta record](../testing/external-beta.md) against the exact tarball. |

## Final release checklist

- [ ] Freeze and record the full release-candidate commit.
- [ ] Confirm the final npm package name, ownership/access, and public metadata.
- [ ] Run lint and formatting checks on the frozen commit.
- [ ] Run unit, component, conformance, type, Nuxt module, production build, and bundle-boundary suites.
- [ ] Prove the oracle and generated conformance artifacts are reproducible.
- [ ] Build the package, inspect its contents, and record the tarball SHA-256.
- [ ] Confirm the tarball ships the project license and authoritative third-party MIT notice required by [provenance](../conformance/provenance.md).
- [ ] Install the exact tarball in two fresh supported Nuxt applications; record environment, commands, and elapsed time under ten minutes for each.
- [ ] Confirm no known data-loss, injection, invalid-document, client-leak, or preview/fixture production-leak defect remains.
- [ ] Complete Gmail web QA with client/browser version and screenshots.
- [ ] Complete Apple Mail QA with platform/client version and screenshots.
- [ ] Complete Outlook for Windows QA with client version and screenshots.
- [ ] Complete and review external beta evidence from a real transactional project.
- [ ] Record accepted limitations and final owner approval.

Unchecked external items are expected during local candidate preparation, but they are release blockers. This record must not be changed to “approved” merely because automated checks pass.
